module.exports = {
  // 1行の最大文字数を指定します。この値を超えると折り返しが行われます。
  printWidth: 120,
  // オブジェクトリテラルや配列リテラルの最後の要素の後にコンマを付けるかどうかを制御します。ES5では末尾のコンマをサポートします。
  trailingComma: 'es5',
  // タブの幅を指定します。このオプションはインデントのスペース数を制御します。
  tabWidth: 2,
  // 文の末尾にセミコロンを付けるかどうかを制御します。
  semi: true,
  // 文字列をシングルクォートで囲むかどうかを制御します。
  singleQuote: true,
  // JSXの属性をシングルクォートで囲むかどうかを制御します。
  jsxSingleQuote: true,
  // 行の終端の文字を指定します。"lf"はUnixスタイルの改行(LF)を意味します。
  endOfLine: 'lf',
};
